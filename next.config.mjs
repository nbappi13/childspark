/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "lh3.googleusercontent.com", // for Google profile images
      "i.imgur.com",               // for course images
    ],
  },
};

export default nextConfig;
